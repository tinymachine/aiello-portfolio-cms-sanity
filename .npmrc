# Use legacy peer dependency resolution to allow Sanity v5.1.0 with plugins that
# haven't updated their peer dependency declarations (e.g., @sanity/orderable-document-list@1.4.1).
# Once these plugins officially support Sanity v5, this can be removed.
legacy-peer-deps=true

